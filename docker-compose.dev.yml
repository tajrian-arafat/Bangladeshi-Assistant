services:
  postgres:
    ports:
      - "5432:5432"

  api:
    volumes:
      - ./backend:/app/backend
    command: uvicorn app.main:app --reload --host 0.0.0.0 --port 8000

  frontend:
    volumes:
      - ./frontend:/app
      - /app/node_modules
    command: npm run dev
