# Automation runtime artifacts (regenerated by orchestrator)
.automation/runs/sim-*/
.automation/reports/*.log
.automation/reports/hook_audit.jsonl
.automation/decisions/sim-*.json
.automation/decisions/dec-*.json

# Keep tracked: project_state.json, batch_queue.json, deployment.lock, workflow.yaml, gates.yaml
# Pilot run artifacts may be committed intentionally under runs/run-*/
