# Publication prompt (LOCAL/DEV ONLY)

Publish only VERIFIED claims with evidence, provenance, freshness, and no unresolved material conflict.

Never publish PARTIALLY_VERIFIED, UNVERIFIED, CONFLICTING, OUTDATED, or REJECTED as authoritative.

Use: `scripts/publish_verified_knowledge.py --dry-run` first.

Write `result.json` when complete.
