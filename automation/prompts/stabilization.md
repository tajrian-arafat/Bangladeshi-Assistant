# Stabilization prompt

Fix reusable routing/language/context issues. No service-specific keyword hacks.

Classify failures: product vs correct uncertainty vs evaluator error.

Write stabilization report under `docs/evaluation/` and `result.json`.
